[pytest]
testpaths = tests
addopts = -ra
filterwarnings =
    error::DeprecationWarning
    ignore::DeprecationWarning:streamlit.*
markers =
    sqlite_only: test depends on SQLite-specific behaviour
